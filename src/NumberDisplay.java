/*public class NumberDisplay {

    private int limit;
    private int value;


    // Constructor / Konstruktør
    public NumberDisplay(int rollOverLimit) {
        this.limit = rollOverLimit; // Definerer grænsen for, hvornår 'value' skal rulle ver til 0
        this.value = 0; // Sætter værdien af 'value' til 0
    }

    // Getter-metode til at bestemme 'value'
    public int getValue() {
        return value;
    }

    // Metode, der viser 'value' som tekst/String
    public int getDisplayValue() {
        if (value < 0) {
            return String("0" + value);
        } else {
            return String("" + value);
        }
    }

    // Setter-metode til at ændre 'value'
    public void setValue(int replacementValue) {
        if ( int replacementValue >=0 && int replacementValue <=limit){
            value = replacementValue; //Opdaterer 'value' med ny værdi
        }
    }

    */


    /* Metode til at bestemme om 'value' er inden for 'limit' og 0
    private int replacementValue(){
            if (int replacementValue >= 0 && int replacementValue <= limit) {
            } value = replacementValue;
    }

    // Metode, som tillader 'value' maksimalt at stige til 'limit'
   /* public void increment() {
            if (value+1 > limit) {
                return limit%24 + value;
            }
    }


    }

*/